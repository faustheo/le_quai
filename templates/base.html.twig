<!doctype html>
<html lang="fr">

<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <meta name="description" content="Le quai antique arrive en Savoie !">
    <meta name="author" content="Mark Otto, Jacob Thornton, and Bootstrap contributors">
    <meta name="generator" content="Jekyll v4.1.1">
    <title>{% block title %}Le Quai Antique {% endblock %}</title>

    <!-- Bootstrap core CSS -->
    <link href="{{ asset('assets/css/bootstrap.min.css') }}" rel="stylesheet">
    <link rel="stylesheet" href="{{ asset('assets/css/le_quai.css') }}">

    <link href="{{ asset('assets/css/carousel.css')}}" rel="stylesheet">
</head>

<body class="d-flex flex-column">
    <header>
        <nav class="navbar navbar-expand-md navbar-dark fixed-top navbar-custom">
            <a class="navbar-brand" href="{{ path('app_home') }}">Le Quai Antique</a>
            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarCollapse"
                aria-controls="navbarCollapse" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarCollapse">
                <ul class="navbar-nav mr-auto">
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_booking') }}">Reservation</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_meals') }}">La carte</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_restaurant_card') }}">Menu</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="#">A propos</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="#">Contact</a>
                    </li>
                </ul>
                <div class="navbar-item-custom">
                    {% if app.user %}
                    <a href="{{ path('app_account') }}">Mon compte <small>({{ app.user.firstname }})</small></a> | <a
                        href="{{ path('app_logout') }}">Déconnexion</a>
                    {% else %}
                    <a href="{{ path('app_login') }}">Connexion</a> | <a
                        href="{{ path('app_register') }}">Inscription</a>
                    {% endif %}
                </div>
            </div>
        </nav>
    </header>

    <main role="main" class="flex-grow-1">

        <!--Si le carousel est définie alors ont l'affiche-->
        {% if block('carousel') is defined %}
        <div id="myCarousel" class="carousel slide" data-ride="carousel">
            <ol class="carousel-indicators">
                <li data-target="#myCarousel" data-slide-to="0" class="active"></li>
                <li data-target="#myCarousel" data-slide-to="1"></li>
                <li data-target="#myCarousel" data-slide-to="2"></li>
            </ol>
            <div class="carousel-inner">
                <div class="carousel-item active img-fluid">
                    <img src="{{ asset('assets/images/carrousel/carou_1.jpg') }}" alt="table intérieur">
                    <div class="container">
                        <div class="carousel-caption text-left purple">
                            <h2>Passez un délicieux moment</h2>
                            <p>
                                Rejoignez-nous pour savourer des plats exquis, une ambiance conviviale et une équipe passionnée. Inscrivez-vous dès maintenant !
                            </p>
                            <p><a class="btn btn-lg btn-custom" href="{{ path('app_register') }}" role="button">Inscrivez-vous</a></p>
                        </div>
                    </div>
                </div>
                <div class="carousel-item img-fluid">
                    <img src="{{ asset('assets/images/carrousel/carou_2.jpg') }}"
                        alt="table de réception">
                    <div class="container">
                        <div class="carousel-caption purple">
                            <h2>Explorez notre menu raffiné et créatif</h2>
                            <p>
                                Laissez-vous tenter par nos délicieuses créations culinaires, préparées avec des ingrédients frais et une touche d'innovation. Bon appétit !
                            </p>
                            <p><a class="btn btn-lg btn-custom" href="{{ path('app_meals') }}" role="button">Voir le menu</a></p>
                        </div>
                    </div>
                </div>
                <div class="carousel-item img-fluid">
                    <img src="{{ asset('assets/images/carrousel/carou_3.jpg') }}"
                        alt="cuisine du restaurant">
                    <div class="container">
                        <div class="carousel-caption text-right purple">
                            <h2>Événements spéciaux et moments inoubliables</h2>
                            <p>
                                Vivez des moments uniques avec nos événements spéciaux : dégustations, soirées à thème et occasions spéciales. Réservez votre place dès maintenant !
                            </p>
                            <p><a class="btn btn-lg btn-custom" href="#" role="button">A propos</a></p>
                        </div>
                    </div>
                </div>
            </div>
            <a class="carousel-control-prev" href="#myCarousel" role="button" data-slide="prev">
                <span class="carousel-control-prev-icon" aria-hidden="true"></span>
                <span class="sr-only">Previous</span>
            </a>
            <a class="carousel-control-next" href="#myCarousel" role="button" data-slide="next">
                <span class="carousel-control-next-icon" aria-hidden="true"></span>
                <span class="sr-only">Next</span>
            </a>

        </div>
        {% endif %}

        <!--Si le carousel n'est pas défini, on ajoute une marge en haut-->
        <div class="container marketing {% if block('carousel') is not defined %}mt-5{% endif %}">
            <!--Corps de la page présent dans index.html.twig-->
            {% block gallery %}
            {% endblock %}
            {% block content %}
            {% endblock %}

        </div>
    </main>
    
    <div class="spacer"></div>
    <!-- FOOTER-->
    <footer class="footer">
        {% block footer %}
            {{ render(controller('App\\Controller\\FooterController::footer')) }}
        {% endblock %}
    </footer>



    <script src="/assets/js/jquery-3.6.0.min.js"></script>
    <script src="//cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    <script src="/assets/js/bootstrap.bundle.js"></script>
    <script src="/assets/js/le_quai.js"></script>
</body>

</html>